<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="hz.com.hz_recyclerview">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".activity.OrdinaryActivity"/>
        <activity android:name=".activity.AddHeaderFooterActivity"/>
        <activity android:name=".activity.AnimationActivity"/>
        <activity android:name=".activity.SectionUseActivity"/>
        <activity android:name=".activity.MultiLayoutActivity"/>
        <activity android:name=".activity.DragActivity"/>
        <activity android:name=".activity.EventActivity"/>
        <activity android:name=".activity.EmptyViewActivity"/>
        <activity android:name=".activity.AdDialogActivity"/>
    </application>

</manifest>